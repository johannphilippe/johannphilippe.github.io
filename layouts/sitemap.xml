{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- $now := now -}}
  {{- range .Pages }}
  {{- /* Agenda events are dated in the future (buildFuture); a document cannot
         have been "last modified" in the future, so clamp lastmod to today. */ -}}
  {{- $lm := .Lastmod -}}
  {{- if gt $lm.Unix $now.Unix }}{{ $lm = $now }}{{ end }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not $lm.IsZero }}
    <lastmod>{{ safeHTML ($lm.Format "2006-01-02T15:04:05-07:00") }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>
    {{- end }}
    {{- if .IsTranslated }}
    {{- range .AllTranslations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode }}" href="{{ .Permalink }}"/>
    {{- end }}
    {{- end }}
  </url>
  {{- end }}
</urlset>
